namespace balducci.jacopo._5I.Briscola.Core
{
    internal class Cpu : Player
    {
        public Card PlayCard(Deck deck, Card previousCard, Card winningCard)
        {
            return Hand.GetCard(0); //DA FARE L'INTELLIGENZA ARTIFICIALE LUL
        }
    }
}